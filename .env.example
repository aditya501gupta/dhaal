# DHAAL — copy to .env and fill. NEVER commit the real .env.
# L1 (day 2+): LLM reasoning + fallback
GROQ_API_KEY=
GEMINI_API_KEY=
# L3 (day 9+): report store
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Server-side secret for HMAC pseudonymisation of scammer indicators (generate: openssl rand -hex 32)
DHAAL_HMAC_SECRET=
